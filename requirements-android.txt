# =======================================================
# Android Termux Optimized Dependencies
# =======================================================
# 1. pydantic v1 avoids Rust compiler (pydantic-core) build failures on Android
# 2. plain uvicorn avoids uvloop C extension build failures
# 4. yfinance omitted: eliminates heavy C/Cython pandas, numpy, and lxml dependencies
# (Pure-Python quote fetcher is built-in via direct requests)

fastapi>=0.100.0,<0.111.0
uvicorn>=0.28.0
sqlalchemy>=2.0.0
openpyxl>=3.1.2
pydantic>=1.10.14,<2.0.0
requests>=2.31.0
youtube-transcript-api>=0.6.2
deep-translator>=1.11.4
python-dotenv>=1.0.0
httpx>=0.27.0
rich>=13.0.0
typer>=0.9.0

